---
description: 项目宪章优先；与 CHARTER.md 冲突时先对齐宪章
alwaysApply: true
---

# 宪章优先

- 先读根目录 `CHARTER.md`，再改代码。
- 本项目是「采集 → 审核 → 公众号发布」编辑流水线，不是洗稿/盗文工具。
- 微信凭证只来自环境变量，禁止写入源码、规则、前端或 git。
- 默认 `AUTO_PUBLISH=false`：采集后进入审核，不直接群发。
- 每篇文章必须有 `source_url`；同一 URL 幂等，禁止重复入库。
