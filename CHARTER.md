# 项目宪章（FitLife Publisher）

> 本文件是最高决策依据。与实现冲突时，先改实现或先修订宪章，禁止 silently 漂移。

## 1. 使命

打造一套**可审核、可追溯、可复用**的内容工作台：从公开、合法的来源采集「健身减脂 / 人生感悟」类素材，经编辑加工后，发布到运营者自己的微信公众号。

本系统是**编辑流水线**，不是盗文机。

## 2. 内容策略与边界

- 重点发现今日头条、小红书、微信公众号中与健身减脂、人生感悟相关的高互动内容。
- 平台数据通过官方接口、已授权数据服务、创作者授权或人工提交公开链接进入系统；保留热度指标及采集时间。
- AI 用于提炼观点、润色表达和生成微信公众号 HTML 排版；不得虚构事实，发布时保留原作者与来源链接。
- 全文再发布必须拥有授权；未获全文授权时，仅保存标题、摘要、链接和编辑团队新增的原创内容。
- 平台凭证与 LLM Key 只通过环境变量提供，不进入仓库、日志、前端或文档。

## 3. 核心原则

1. **领域独立**：业务规则活在 `domain` / `application`，不依赖 FastAPI、SQLAlchemy、Vue。
2. **依赖朝内**：基础设施实现端口（Port），组合根负责装配。
3. **用例即事务边界**：一个用户意图 = 一个应用服务方法。
4. **默认安全**：密钥只走环境变量；默认不自动群发。
5. **先审核后发表**：采集 → 评分 → 人工/规则审核 → 草稿 → 发布。
6. **来源可追溯**：每篇文章必须保留 `source_url` 与来源标识。
7. **幂等**：同一 `source_url` 不重复入库；同一篇文章不重复向微信提交。
8. **失败可见**：外部调用失败要有结构化错误，禁止吞异常。
9. **简单胜过聪明**：不为未来假想需求引入 CQRS、事件总线、微服务。
10. **接口稳定、实现可换**：换 RSS 实现、换公众号 SDK、换数据库，不应改领域模型。

## 4. 限界上下文

| 上下文 | 职责 | 禁止越界 |
|---|---|---|
| Ingestion | 来源管理、抓取、去重、质量初筛 | 不直接调微信 |
| Editorial | 改写、摘要、封面、审核状态 | 不关心 HTTP |
| Publishing | 微信草稿与群发、发布记录 | 不重新抓取网页 |
| Identity/Config | 运行配置、凭证 | 不承载业务规则 |

上下文之间只通过应用服务编排，禁止跨模块直接读写对方表结构（仓储接口除外）。

## 5. 质量闸门

- 合并前：后端至少覆盖「去重入库」与「发布编排」两条路径的测试。
- 发布前：文章状态必须是 `approved`；必须有标题与正文。
- 自动群发必须显式打开 `AUTO_PUBLISH=true`，且仍写发布审计日志。

## 6. 变更纪律

- 新增依赖必须说明替代了哪一层的哪一个端口。
- 破坏领域不变量必须先改宪章再改代码。
- 密钥轮换不需要发版：只改环境变量。
